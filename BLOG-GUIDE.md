# คู่มือเขียนบล็อก — The Green Journal

กติกาที่ใช้กับทุกบทความในเว็บนี้ อ้างอิงจากงานวิจัย SEO ปี 2026 และจากตัวเลขที่วัดจากบทความของเราเอง
เป้าหมายของบล็อก: **ให้คนที่ค้นหาเรื่องสุขภาพเจอเรา แล้วรู้จักสินค้าไปด้วยโดยไม่รู้สึกว่าถูกขาย**

---

## 1. ความยาว

| ประเภทบทความ | ตัวอักษรไทย | เทียบคำอังกฤษ |
|---|---|---|
| บทสนับสนุน (ปกติ) | **7,000–9,000** | 1,500–2,000 |
| บท Pillar (เสาหลัก เช่น วิตามินบี 12) | 10,000–11,000 | 2,300–2,500 |
| สั้นเกินไป — อย่าปล่อยลง | ต่ำกว่า 3,000 | ต่ำกว่า 700 |

อัตราแปลง **1 คำอังกฤษ ≈ 4.5 ตัวอักษรไทย** (วัดจากบท B12 ซึ่งมีครบสองภาษา: 10,313 ตัวอักษรไทย = 2,299 คำอังกฤษ)

> **ห้ามเขียนยืดให้ถึงเป้า** งานวิจัยตรงกันว่าเกิน 3,500 คำผลตกลง บทยาวติดอันดับเพราะครอบคลุมหัวข้อย่อยได้มาก
> ไม่ใช่เพราะยาว ถ้าเนื้อหาหมดที่ 6,000 ตัวอักษรก็จบที่ 6,000 อย่าเติมน้ำ

**วิธีนับ**

```bash
python3 - <<'PY'
import re, sys
s = open(sys.argv[1] if len(sys.argv)>1 else 'blog/ARTICLE.html', encoding='utf-8').read()
body = s.split('<div class="breadcrumb-hero">')[1].split('<footer')[0]
n = sum(len(re.sub(r'<[^>]+>', '', x)) for x in re.findall(r'data-th(?:-html)?="([^"]+)"', body))
print('ตัวอักษรไทยในเนื้อหา:', n)
PY
```

## 2. ภาพ

**เป้าหมาย 5–7 ภาพต่อบท** (ค่าเฉลี่ยของบทความที่ performance ดีคือ 5.7 ภาพ)

| ประเภท | จำนวน | ใครทำ |
|---|---|---|
| ภาพปก (ภาพถ่าย + ตัวอักษรทับ) | 1 | Claude สร้างได้ |
| ตาราง / อินโฟกราฟิก / แผนภาพขั้นตอน | 3–4 | Claude สร้างได้ |
| ภาพถ่ายจริง (ฟาร์ม สินค้า อาหาร) | 1–2 | **เจ้าของต้องส่งมา** |

**กฎเหล็ก**

- **ห้ามใช้ภาพซ้ำกันในบทเดียว** — รวมถึงภาพปกที่ครอปมาจากภาพในเนื้อหา ตาคนมองว่า "ภาพเดียวกัน" ไม่สนว่าคนละไฟล์
- **ย่อภาพก่อนอัปโหลดเสมอ — ด้านยาวไม่เกิน 1,800px** ภาพจากกล้องขนาดเต็มทำให้หน้าเว็บหนักเป็นสิบเมกะไบต์ และ Google ใช้ความเร็วเว็บจัดอันดับ
- ภาพที่เราสร้างเอง (original) มีค่ากับ SEO มากกว่าภาพสต็อก และภาพถ่ายจริงจากฟาร์มคือของที่คู่แข่งลอกไม่ได้

**เช็กภาพซ้ำ**

```bash
python3 - <<'PY'
import re, hashlib, os, sys
s = open(sys.argv[1] if len(sys.argv)>1 else 'blog/ARTICLE.html', encoding='utf-8').read()
body = s.split('<div class="breadcrumb-hero">')[1].split('<footer')[0]
seen = {}
for i, f in enumerate(re.findall(r'<img src="\.\./(assets/images/[\w.-]+)"', body), 1):
    h = hashlib.md5(open(f,'rb').read()).hexdigest()[:8]
    print('%d. %-34s %s' % (i, os.path.basename(f), 'ซ้ำกับภาพที่ %d' % seen[h] if h in seen else ''))
    seen.setdefault(h, i)
PY
```

**ชื่อไฟล์** ใช้ `journal-NN.jpg` สำหรับปก และ `journal-NN-<ชื่อ>.jpg` สำหรับภาพประกอบในบท
เช่น `journal-08.jpg`, `journal-08-compare.jpg`

> เวลาสร้างภาพกราฟิกด้วย HTML + Playwright **ต้องใส่ `<meta charset="utf-8">` บรรทัดแรกเสมอ**
> ไม่งั้นตัวอักษรไทยจะเพี้ยนเมื่อไฟล์มีภาพ base64 ขนาดใหญ่ (เบราว์เซอร์เดารหัสอักขระจากช่วงต้นไฟล์เท่านั้น)

## 3. บทนำและประโยคฮุก

โครง **Hook → Transition → Promise**

1. **Hook** — สถิติที่ทำให้สะดุด / คำถามที่โดนใจ / ความเชื่อผิด ๆ ที่จะหักล้าง
2. **Transition** — เชื่อมว่าทำไมเรื่องนี้เกี่ยวกับผู้อ่าน
3. **Promise** — อ่านจบแล้วจะได้อะไร

**สำคัญที่สุด: Answer-first** ตอบคำถามหลักให้จบใน 2–3 บรรทัดแรก ห้ามเกริ่นยาว
เพราะ AI Overview ของ Google ดึงประโยคแรก ๆ ไปอ้างอิง เกริ่นน้ำ 5 บรรทัด = ถูกข้ามไปหาเว็บอื่น

ผลที่วัดได้จากบทความที่เขียนบทนำใหม่: bounce 72% → 38% · เวลาอยู่บนหน้า 22 วินาที → 1 นาที 14 วินาที · อ่านลึก 18% → 61%

## 4. ชื่อบทความ

**คำหลักมาก่อน ลูกเล่นมาทีหลัง** คนไทยค้นหาแบบตรงตัวมาก ถ้าชื่อบทไม่มีคำที่เขาพิมพ์ Google จะไม่จับคู่

- ✅ `ไข่ผำกินยังไง? คู่มือฉบับคนปลูก ตั้งแต่ล้าง เก็บ ไปจนถึง 8 เมนูที่ทำได้จริง`
- ✅ `วิตามินบี 12: วิตามินที่ร่างกายต้องการน้อยที่สุด แต่ขาดแล้วเสียหายมากที่สุด`
- ❌ `เปิดโลกซูเปอร์ฟู้ดที่คุณไม่เคยรู้จัก` — สวยแต่ไม่มีใครค้นหาคำนี้

## 5. กฎ อย. — เส้นที่ห้ามข้าม

สินค้าเราจดทะเบียนเป็น **อาหาร** ไม่ใช่ยา ตาม พ.ร.บ. อาหาร จึงมีเส้นแบ่งชัดเจน

| ❌ ห้ามเขียน | ✅ เขียนได้ |
|---|---|
| รักษา / บำบัด / ป้องกันโรค / หายจาก | "มีส่วนช่วยในการทำงานตามปกติของระบบประสาท" |
| ลดน้ำหนัก / ลดความอ้วน | "0 แคลอรี" / "โปรตีน X กรัม" |
| รีวิวผู้ใช้แบบ "กินแล้วหาย" / ภาพ before-after | ผลแล็บที่มีเลขที่รายงานอ้างอิงได้ |
| "สูง" "ที่สุด" ที่ไม่มีตัวเลขรองรับ | ตัวเลขที่ยืนยันได้จากฉลากหรือผลแล็บ |

> คำว่า "สูง" (เช่น "โพแทสเซียมสูง") คือการกล่าวอ้างปริมาณสารอาหาร ต้องมีตัวเลขผ่านเกณฑ์รองรับ
> ถ้าไม่มีตัวเลข ให้ใช้ "มี...ตามธรรมชาติ" แทน

**หลักคิด:** เขียนเรื่อง "บี 12 คืออะไร" = ให้ความรู้ ทำได้เต็มที่ · เขียนว่า "ไข่ผำเราแก้อาการชาปลายมือ" = โฆษณาสรรพคุณ ผิดกฎทันที

⚠️ ข้อความที่จะใช้จริง โดยเฉพาะที่ส่งออกญี่ปุ่น/อเมริกา ควรให้ที่ปรึกษาด้าน อย. ตรวจอีกรอบ

## 6. ขายแบบไม่ขาย — สัดส่วน 90 / 10

- **เนื้อหา 90%** = ให้ความรู้ล้วน ไม่พูดถึงสินค้าเลย
- **ท้ายบท 10%** = ข้อเท็จจริงว่าเราทำอะไร (ฟาร์มระบบปิด ตรวจแล็บทุกเดือน ซื้อได้ที่ไหน) + กล่อง CTA

**เทคนิคที่ได้ผลดีที่สุด: "หัวข้อที่คู่แข่งตอบไม่ได้"**
เช่นหัวข้อ "4 คำถามที่ควรถามคนขายก่อนซื้อ" — เราตอบได้ครบทุกข้อเพราะเราปลูกเอง
แต่คนที่ช้อนจากแหล่งน้ำธรรมชาติตอบไม่ได้ นี่คือการขายโดยไม่ต้องบอกให้ซื้อของเรา

## 7. เลือกหัวข้อ — Evergreen 80% / ตามฤดูกาล 20%

- **80% Evergreen** — "ไข่ผำกินยังไง" "บี 12 คืออะไร" คนค้นทุกเดือน ลงแรงครั้งเดียวกินยาว
- **20% ตามฤดูกาล** — กินเจ/เข้าพรรษา (คนหาโปรตีนพืชพุ่ง) ปีใหม่ (ตั้งเป้าสุขภาพ) หน้าร้อน

เว็บเรายังใหม่ ยังไม่มี authority พอจะแข่งเรื่องฮิตกับเว็บใหญ่
ให้เน้นคำเฉพาะทางที่เราชนะได้เพราะ **เราคือคนที่ปลูกจริง** — งานวิจัย YMYL ยืนยันว่า Trust สำคัญกว่าความยาว

## 8. ตัวเลข — ยังไม่ยืนยัน = ไม่ลง

ถ้าไม่มีเอกสารยืนยัน ให้เขียนเลี่ยงไปก่อน เช่น "ดูปริมาณต่อหน่วยบริโภคได้จากฉลากบนบรรจุภัณฑ์"
แล้วค่อยใส่ตัวเลขจริงทีหลัง **ดีกว่าลงตัวเลขผิดแล้วต้องตามแก้**

บทเรียนจริง: หน้าผลิตภัณฑ์เคยระบุ "ต่อ 1 ซอง (5 ก.) — 16 kcal, โพแทสเซียม 380 มก."
พอเปลี่ยนสูตรเป็นซองละ 0.5 กรัม ตัวเลขทั้งแถวผิดไป 10 เท่าโดยไม่มีใครสังเกต

**เวลาสินค้าเปลี่ยนสูตร ให้ไล่หาตัวเลขเก่าทั้งเว็บเสมอ**

```bash
grep -rn "กรัม\|มก\.\|กิโลแคลอรี\|kcal" --include=*.html .
```

## 9. เรื่องเทคนิคที่ต้องผ่านทุกครั้งก่อนขึ้นเว็บ

- [ ] ทุก `data-th` ต้องมี `data-en` คู่กัน (ถ้าขาด ข้อความจะค้างเป็นไทยตอนสลับภาษา)
- [ ] `data-th-html` ต้องคู่กับ `data-en-html` (ใช้เมื่อมี `<strong>` ในข้อความ)
- [ ] แท็ก HTML ปิดครบ
- [ ] ไม่มีภาพเสีย และไม่มีภาพซ้ำ
- [ ] สลับ EN แล้วไม่เหลือข้อความไทยค้าง
- [ ] เพิ่มการ์ดบทความใน `blog.html` (การ์ดใหม่อยู่บนสุด)
- [ ] ลิงก์ไขว้ไปยังบทความอื่นที่เกี่ยวข้องอย่างน้อย 1 จุด

## 10. ขั้นตอนการทำงาน

```
เจ้าของบอกหัวข้อ
      ↓
Claude เขียน + ส่งลิงก์พรีวิว (ยังไม่แตะเว็บจริง)
      ↓
เจ้าของอ่าน บอกจุดที่อยากแก้
      ↓
แก้แล้วส่งลิงก์เดิมให้ดูใหม่ (แก้กี่รอบก็ได้)
      ↓
เจ้าของพิมพ์คอนเฟิร์ม
      ↓
ขึ้นเว็บจริง
```

**จะไม่มีอะไรขึ้นเว็บจนกว่าเจ้าของจะคอนเฟิร์ม** และกฎนี้ใช้กับทุกอย่างที่เป็นการลบหรือเปลี่ยนของเดิมด้วย ไม่ใช่แค่บล็อก

---

## ที่มาของตัวเลขในคู่มือนี้

| ตัวเลข | แหล่ง |
|---|---|
| บทที่ติดอันดับเฉลี่ย 1,447 คำ | Backlinko |
| Google top 10 เฉลี่ย 2,400 คำ | รวบรวมโดย seo.co |
| เนื้อหา performance ดี 1,152 คำ / แย่ 668 คำ | Semrush |
| บทที่ performance ดีมีภาพเฉลี่ย 5.7 ภาพ | Venngage |
| อัปเดตภาพ original แล้ว traffic +134% (หมวดสุขภาพ +189%) | HubSpot (ทดสอบกับ 3,400 บทความ) |
| Google Images = 22% ของการค้นหาทั้งหมด | สถิติ Image SEO 2026 |
| แก้บทนำแล้ว bounce 72% → 38% | ClickRank |
| หมวดสุขภาพแกว่ง 20% หลังอัปเดตอัลกอริทึม มิ.ย. 2026 | รายงาน E-E-A-T / YMYL 2026 |
